import 'package:flutter/material.dart';

class DeliveringOrders extends StatelessWidget {
  const DeliveringOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Delivering orders'),
    );
  }
}
